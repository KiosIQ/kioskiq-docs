name: Publish Docs

on:
  push:
    branches: [main]
    paths:
      - 'site/**'
      - '.github/workflows/pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Build and deploy to GitHub Pages
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Verify every page is valid HTML we can serve
        run: |
          set -euo pipefail
          missing=0
          for f in site/index.html site/guides/till/index.html site/guides/kitchen/index.html; do
            if [ ! -s "$f" ]; then echo "::error::missing or empty: $f"; missing=1; fi
          done
          if ! grep -qi '<!doctype html>' site/index.html; then
            echo "::error::site/index.html has no doctype"; missing=1
          fi
          for f in site/guides/till/index.html site/guides/kitchen/index.html; do
            if ! grep -qi '<!doctype html>' "$f"; then
              echo "::error::$f has no doctype"; missing=1
            fi
            if ! grep -qi 'charset="utf-8"' "$f"; then
              echo "::error::$f has no charset - non-ASCII text will render as mojibake"; missing=1
            fi
          done
          exit $missing

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          # Provision the Pages site on first run instead of requiring
          # someone to flip Settings > Pages by hand. Needs the repository
          # to be eligible for Pages: public, or private on a paid plan.
          enablement: true

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
